const slides=document.querySelectorAll(".slide"),pagination=document.querySelector(".pagination");pagination.innerHTML='<span class="dot"></span>'.repeat(slides.length);const dots=document.querySelectorAll(".dot");let currentIndex=0;function showSlide(e){const t=-100*e;slides.forEach((e=>{e.style.transform=`translateX(${t}%)`}))}function setActiveDot(e){dots.forEach(((t,n)=>{t.classList.toggle("active",n===e)}))}function showNextSlide(){currentIndex=(currentIndex+1)%slides.length,showSlide(currentIndex),setActiveDot(currentIndex)}setActiveDot(currentIndex),dots.forEach(((e,t)=>{e.addEventListener("click",(()=>{currentIndex=t,showSlide(currentIndex),setActiveDot(currentIndex)}))})),setInterval(showNextSlide,2500);
//# sourceMappingURL=script.js.map